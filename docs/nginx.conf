server {
    listen 80;

    root /usr/share/nginx/html;
    charset utf-8;

    # Serve .md files as plain text so browsers and agents can read them
    types { }
    default_type text/plain;

    location / {
        autoindex on;
    }
}
